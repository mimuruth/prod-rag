# Generation LLM
OPENAI_API_KEY=

# Reranking (optional — falls back to local cross-encoder if unset)
COHERE_API_KEY=

# Observability (self-hosted Langfuse)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://localhost:3000
